package com.example.security.entity;

/**
 * The Role enum represents the roles that can be assigned to a user.
 * It is used to define the role of a user in the system.
 */
public enum Role {
    ROLE_USER, ROLE_ADMIN, ROLE_MANAGER
}
